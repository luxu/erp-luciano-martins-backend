[pytest]
DJANGO_SETTINGS_MODULE = erp_luciano_martins.settings
python_files = test_*.py
python_functions = test_*
addopts =
    --reuse-db
    --nomigrations -p no:warnings
filterwarnings = ignore::DeprecationWarning